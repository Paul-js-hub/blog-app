<h1>This page displays a list of all users</h1>

<% @users.each do |user| %>
 <div>
 <p>User photo</p>
 </div>
 <div style='border: 1px solid gray'user_path(user.id)>
 <h3>Name: <%= link_to user.name, user_path(user.id) %></h3>
 <p>Number of posts: <%= user.posts_counter %></p>
 </div>
<% end %>